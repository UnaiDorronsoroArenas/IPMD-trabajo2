apiVersion: 1

providers:
  - name: 'clusters'
    orgId: 1
    folder: 'clusters'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards